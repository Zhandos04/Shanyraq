spring:
  application:
    name: Turamyzba

  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/turamyzba
    username: postgres
    password: Codemedy175A369_

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 1000MB

  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:5432/turamyzba
    user: test
    password: 12345678
    locations: classpath:db/migration

  mail:
    host: smtp.gmail.com
    port: 587
    username: turamyzba@gmail.com
    password: wasn ftdo frbe isjb
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

2gis:
  api:
    key: ${2GIS_API_KEY}
    url: https://catalog.api.2gis.com/3.0/items

aws:
  accessKey: ${AWS_ACCESS_KEY_ID}
  secretKey: ${AWS_SECRET_ACCESS_KEY}
  s3:
    region: ${AWS_REGION}
    bucket: ${S3_BUCKET_NAME}

jwt:
  secret: f20be2a4a63bb8c9f2b5ecbed7f0971a96d66e30f3f7c76b56a6e0361ea8e76a

server:
  servlet:
    context-path: '/api/'
  error:
    include-message: always
  port: ${PORT:8080}
  address: 0.0.0.0